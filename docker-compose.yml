

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
    command: npm run dev
    networks:
      - appnet

networks:
  appnet:
    driver: bridge